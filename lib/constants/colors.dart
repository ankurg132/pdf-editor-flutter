import 'package:flutter/material.dart';

class MyColors {
  static const primaryColor = Color.fromRGBO(255, 0, 102, 1);
  static const backColor = Color.fromRGBO(255, 230, 242, 1);
  // static const primaryColor = Color.from;
}
